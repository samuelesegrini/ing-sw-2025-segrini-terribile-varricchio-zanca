/**
 * The adventure cards themselves, one file each.
 *
 * <p>A card owns its own rules and hands back its own resolution. Nothing here switches
 * over card types, which is what lets a new card be a new file rather than an edit to
 * something that already handles ten others.
 */
package it.polimi.ingsw.server.model.adventure.card;
