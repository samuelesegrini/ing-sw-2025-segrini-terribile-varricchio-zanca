/**
 * Adventure cards: what is printed on them and how a flight draws them.
 *
 * <p>Only the identity of a card lives here for now — its type, its level and whether
 * it bears the test flight mark — which is what deck building needs. The payload of
 * each card type arrives with its resolution, one card at a time.
 */
package it.polimi.ingsw.server.model.adventure;
